<!DOCTYPE html>
<html lang="en">

<%- include('partials/head.ejs', {title: "Home" }) %>





    <body class="bg-gray-75">
        <%- include('partials/header.ejs', {name: name }) %>
            <!-- Main Content -->
            <main class="px-24 mt-24">
                <div>
                    <!-- Brainstorm Buttons in One Row -->
                    <div class="flex justify-end gap-4 mb-4">
                        <!-- Brainstorm 1 Button -->
                        <a href="/brainstorm1/search" class="relative group">
                            <button class="bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700">
                                Brainstorm +
                            </button>
                            <span
                                class="absolute top-full mb-2 hidden group-hover:block w-64 bg-gray-400 text-white text-xs p-2 rounded-md shadow-md">
                                Brainstorm using Wikipedia, AI, and force-directed graphs for passive
                                brainstorming.
                            </span>
                        </a>

                        <!-- Brainstorm 2 Button -->
                        <a href="/brainstorm2/create-lab" class="relative group">
                            <button class="bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700">
                                Brainstorm 2 +
                            </button>
                            <span
                                class="absolute top-full mb-2 hidden group-hover:block w-64 bg-gray-400 text-white text-xs p-2 rounded-md shadow-md">
                                Actively add your ideas and let AI describe their relationships using
                                force-directed graphs.
                            </span>
                        </a>

                        <!-- Brainstorm 3 Button -->
                        <a href="/brainstorm3/create-lab" class="relative group">
                            <button class="bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700">
                                Brainstorm 3 +
                            </button>
                            <span
                                class="absolute top-full mb-2 hidden group-hover:block w-64 bg-gray-400 text-white text-xs p-2 rounded-md shadow-md">
                                Brainstorm with AI and force-directed graphs using images.
                            </span>
                        </a>
                    </div>

                </div>
            </main>


            <!-- Welcome Modal  -->
            <div id="initialModal"
                class="hidden fixed inset-0 flex items-center justify-center bg-gray-900 bg-opacity-75 z-50">
                <div class="bg-white rounded-xl shadow-2xl p-8 max-w-3xl w-full text-center relative overflow-hidden">
                    <!-- Decorative Top Bar -->
                    <div class="absolute top-0 left-0 right-0 h-2 bg-gradient-to-r from-blue-500 to-blue-700">
                    </div>

                    <!-- Modal Content -->
                    <h2 class="text-3xl font-extrabold text-gray-800 mb-4">🚀 Welcome to Brainstorm AI!
                    </h2>
                    <p class="mb-6 text-gray-600 text-lg">
                        Brainstorm AI's Goal is to transform how users brainstorm ideas.
                        Get started by familiarizing Brainstorm AI features:
                    </p>
                    <div class="bg-gray-50 rounded-lg p-6 mb-6 shadow-inner border border-gray-200">
                        <ul class="text-left list-disc list-inside text-gray-700 space-y-4 text-base">
                            <li><strong>Brainstorm1: </strong> Navigate Wikipedia articles and let AI brainstorm
                                them for you.</li>
                            <li><strong>Brainstorm2: </strong> Add Ideas and let the system brainstorm them for
                                you.</li>
                            <li><strong>Brainstorm3: </strong> Upload image and let the system brainstorm them
                                for you.</li>
                        </ul>
                    </div>

                    <!-- Call-to-Action Button -->
                    <button id="closeModal"
                        class="bg-gradient-to-r from-blue-500 to-blue-600 text-white font-semibold px-8 py-3 rounded-full shadow-md hover:from-blue-600 hover:to-blue-700 hover:shadow-lg transition-transform transform hover:scale-105">
                        Got it!
                    </button>

                    <!-- Decorative Bottom Bar -->
                    <div class="absolute bottom-0 left-0 right-0 h-2 bg-gradient-to-r from-blue-500 to-blue-700">
                    </div>
                </div>
            </div>

            <!-- Welcome Script -->
            <script>
                // Function to extract query parameters from the URL
                function getQueryParameter(name)
                {
                    const urlParams = new URLSearchParams(window.location.search);
                    return urlParams.get(name);
                }

                document.addEventListener('DOMContentLoaded', () =>
                {
                    const initial = getQueryParameter('initial');
                    const modal = document.getElementById('initialModal');
                    const closeModal = document.getElementById('closeModal');

                    if (initial === 'true')
                    {
                        modal.classList.remove('hidden');
                    }

                    closeModal.addEventListener('click', () =>
                    {
                        modal.classList.add('hidden');
                    });
                });
            </script>

    </body>

</html>